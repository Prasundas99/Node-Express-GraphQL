# Node Express GraphQL

Simple Demo Application

**API** built with Node + Express + GraphQL + MongoDb. 

## Core Structure
    node-express-graphql
      ├── api (api.example.com)
      │   ├── src
      │   │   ├── config
      │   │   ├── models
      │   │   ├── schema
      │   │   ├── setup
      │   │   └── index.js
      │   │
      │   └── package.json
      │
      ├── .gitignore
      └── README.md

## License ##

MIT
